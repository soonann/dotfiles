- name: Install system dependencies
  become: true
  ansible.builtin.dnf:
    state: latest
    name:
      - polybar
      - rofi
      - lxappearance

      - xinput
      - xset

      - arandr
      - xrandr 

      - blueman

      - pamixer
      - light

- name: Link X11 dotfiles
  ansible.builtin.shell: stow * -t $HOME
  args:
    chdir: "{{ role_path }}/files/x11"
